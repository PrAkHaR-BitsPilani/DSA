#include<stdio.h>'
#include<string.h>

struct node{
    char c;
    struct node* forward;
    struct node* backward;
};


int main(){
    int n;
    scanf("%d" , &n);
    char s[1005];
    scnaf("%s" , s);
    int len_s = strlen(s);
    return 0;
}